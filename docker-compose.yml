services:
  assignment5:
    build: .
    ports:
      - "8000:80"
    volumes:
      - ./models:/app/models
      - ./.cache:/app/.cache
    environment:
      - TRANSFORMERS_CACHE=/app/.cache/huggingface
      - HF_HOME=/app/.cache/huggingface
